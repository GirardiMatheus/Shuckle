# ============================
# Configuração do Firewall
# ============================

# Portas TCP essenciais que devem ser permitidas (separadas por espaço)
# Exemplo: "22 80 443"
ALLOWED_PORTS="22 80 443"

# Portas adicionais a serem bloqueadas (separadas por espaço)
# Exemplo: "23 21 137 138 139 445"
BLOCKED_PORTS="23 21 137 138 139 445"

# Interface de rede para aplicar as regras (use 'eth0' para Ethernet ou 'wlan0' para Wi-Fi)
NETWORK_INTERFACE="eth0"

# Lista de IPs confiáveis que sempre terão acesso (separados por espaço)
# Exemplo: "192.168.1.100 10.0.0.5"
TRUSTED_IPS="192.168.1.100 10.0.0.5"

# Ativar log para tentativas de acesso bloqueado? (yes/no)
LOG_DROPPED="yes"

# Arquivo onde as regras do iptables serão salvas para restauração futura
IPTABLES_SAVE_FILE="/etc/iptables.rules"

# Ativar saída detalhada do script? (yes/no)
VERBOSE="no"

# Política padrão para pacotes desconhecidos (ACCEPT/DROP)
DEFAULT_POLICY="DROP"
